name: ci-acme

on: pull_request

jobs:
  check-app:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2  
    - name: build image
      run:  make devbuild
    - name: Verify image
      run:  docker ps
    - name: Check lint
      run:  make shortlint
    - name: Check lint
      run:  make lint

